import * as applicationRequests from './application';
import u from './users';
import * as userRequests from './user';

export const application = applicationRequests;
export const users = u;
export const user = userRequests;
